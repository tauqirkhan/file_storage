const postUpload = async (req, res) => {
  console.log("Do nothing right now");
  console.log("Need to integrate the multer middleware");
  console.log(
    "We will upload these files once we have all other features working!"
  );
};

module.exports = postUpload;
